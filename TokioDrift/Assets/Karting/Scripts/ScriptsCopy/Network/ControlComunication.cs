using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlComunication : MonoBehaviour
{
    //Some references
    /*
    [Header("Reference of comuncication")]
    [SerializeField] private ComunicationStates instanceOfCE= null;
    
    private void onEnable()
    {
        instanceOfCE.EventChangeSomeOrbe += HandleChangeOfOrbe;
        instanceOfCE.EventChangeSomeTrack += HandleChangeOfOrbe;
    }

    private void onDisable()
    {
        instanceOfCE.EventChangeSomeOrbe -= HandleChangeOfOrbe;       
        instanceOfCE.EventChangeSomeTrack -= HandleChangeOfOrbe;
    }

    private void HandleChangeOfOrbe(int identfication, bool state)
    {
        //change my orbs and track with this new information 
    }

    private void HandleChangeOfTrack(int identfication, bool state)
    {
        //change my orbs and track with this new information 
    }    
        */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
